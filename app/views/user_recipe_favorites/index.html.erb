<div class="col-sm-12">
  <h2>Favorites of <%= @user_name %>
  <%= link_to(image_tag('add-icon.png', size:'16', title: 'Add recipe'), new_recipe_path) if user_signed_in? %>
</h2>
  <%= render partial: "recipes/recipes", locals: {recipes: @recipes} %>
</div>
